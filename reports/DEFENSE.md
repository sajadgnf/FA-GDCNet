# کارت دفاع — FA-GDCNet

اعداد از `reports/REPORT.md` (n=1299، CLIP چهره روی همهٔ ردیف‌های ارزیابی، خط پایهٔ تک‌وجهی روی همان شناسه‌ها). فرضیه‌ها §6.3 پروپوزال‌اند.

## سه فرضیهٔ PDF

1. **حافظه < ۱ گیگابایت** — اوج مرحله‌ای **0.945 GiB**. داشبورد حدود **4.2 GiB**. بودجه روی استخراج مرحله‌ای است، نه UI.
2. **دقت تشخیص کنایه > ۷۰٪** — دقت clash **79.5٪** برابر dummy «همیشه غیرکنایه» است. Precision/Recall/F1 **0.50 / 0.48 / 0.49**. روی کپشن‌های ارگانیک (۱۰۰ کنایه) clash F1 **0.33** است؛ روی recaption ساخته‌شده **0.87**. تشخیص روی دامنهٔ §5 ضعیف‌تر از عدد مخلوط است.
3. **نسبت به مدل سنگین** — **حل نشد**. یک اجرای ۴نمونه‌ای Qwen2-VL-2B: افت دقت **0.25**، ما سریع‌تر و زیر ۱ گیگ. n=۴ آزمون فرضیه نیست. n=۱۵۰ به‌خاطر page file لود نشد.

سؤال پژوهشی ۲: Δ کنایه مقابل تک‌وجهی **+24.5** امتیاز (0.468 در برابر 0.222) روی همان 1299 نمونه. حد ۱۰ امتیاز رد می‌شود. خط پایه تصویر نمی‌بیند.

## §5 برچسب

- کنایه‌های طلایی **268** (171 مثبت / 97 منفی)، همگی `blind-relabel`. jsonl **1324**؛ **25** تصویر گم → ارزیابی **1299**.
- **212** ردیف recaption ساخته‌شده (**166** کنایه) در ارزیابی هستند؛ در پایان‌نامه باید افشا شوند.
- کپا **0.778** روی **230** پست همپوشان (`reports/iaa.md`). بقیهٔ jsonl دو-برچسب کور نیست.

## Ablation

`aux_only` sarcasm-F1 **0.419**؛ مدل کامل **0.468**؛ `Dsem+Dsen` **0.464**. `no_clip` **0.292** — تشخیص زیرنوع به قطبیت چهرهٔ CLIP وابسته است. سهم Dsem/Dsen روی F1 کنایه دیده می‌شود، نه روی دقت ۵کلاسه در برابر dummy اکثریت.

## انحراف از متن پروپوزال

- قطبیت متن: twitter-xlm-roberta، نه ParsBERT.
- `polarity_T_hat`: CLIP خنده/غم، نه قطبیت جملهٔ SmolVLM.
- کنایهٔ ضمنی / دانش عمومی (§8.2 سناریو ۲) ارزیابی نشده.
