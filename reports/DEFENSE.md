# کارت دفاع — FA-GDCNet

اعداد از `reports/REPORT.md` و `reports/heavy_compare.json`. فرضیه‌ها همان §6.3 پروپوزال‌اند، نه جدول قدیمی STATUS.

## سه فرضیهٔ PDF

1. **حافظه < ۱ گیگابایت** — اوج مرحله‌ای **0.945 GiB**. داشبورد با همهٔ مدل‌ها روی GPU حدود **4.2 GiB** است. بودجه روی استخراج مرحله‌ای است.
2. **دقت تشخیص کنایه > ۷۰٪** — قانون Dsem **85.9٪**. dummy «همیشه غیرکنایه» حدود **88.5٪** است. F1 باینری **0.46**. عبور از ۷۰٪ به معنی بهتر بودن از dummy نیست.
3. **نسبت به مدل سنگین: سریع‌تر، حافظه <۱ GiB، افت دقت <۵٪** — **اندازه‌گیری نشد**. Qwen2-VL-2B-Instruct هنگام بارگذاری روی این سیستم crash کرد (۰xC0000005 روی CUDA و CPU). نتیجه **NOT_RUN** است، نه PASS.

سؤال پژوهشی ۲ (Δ کنایه مقابل تک‌وجهی ≥۱۰ امتیاز) فرضیهٔ ۳ نیست. عدد فعلی حدود **+47** امتیاز است روی طلای عمدتاً retag.

## §5 برچسب

طلای کنایه تا بازبینی دستی ۱۳۷ پست، مستقل نیست. `python tasks.py relabel --only sarcasm` — Enter یعنی تأیید برچسب فعلی.

## Ablation

`aux_only` ≈ تمام شش ویژگی. سهم Dsem/Dsen در کیفیت الماس پنج‌کلاسه **ثابت نشده**.

## انحراف از متن پروپوزال (بگو اگر پرسیدند)

- قطبیت متن: twitter-xlm-roberta، نه ParsBERT.
- `polarity_T_hat`: CLIP خنده/غم، نه قطبیت جملهٔ SmolVLM.
- کنایهٔ ضمنی / دانش عمومی (§8.2 سناریو ۲) ارزیابی نشده.
- IAA: کپا تعریف نشده.
